% !TEX root = ../../main.tex
% WebSocket 握手及订单通信时序示意图
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  arr/.style   = {-Stealth, thick},
  lbl/.style   = {font=\scriptsize},
  hlbl/.style  = {font=\small\bfseries},
  phaselbl/.style = {font=\small\itshape, text=gray!70!black},
  dashline/.style = {dashed, thick, gray!50},
  scale=0.92, every node/.style={scale=0.92},
]

% 生命线 — 间距从6缩小到5.5
\draw[thick] (0, 0) -- (0, -10.0);
\draw[thick] (5.5, 0) -- (5.5, -10.0);
\draw[thick] (11,0) -- (11,-10.0);

% 角色标题
\node[hlbl, above=0.15cm] at (0,  0) {客户端};
\node[hlbl, above=0.15cm] at (5.5,  0) {服务器};
\node[hlbl, above=0.15cm] at (11, 0) {订单房间};

% ---- Phase 1: HTTP 升级握手 ----
\draw[dashline] (-1.6,-0.8) -- (12.6,-0.8);
\node[phaselbl, right] at (-1.6,-0.5) {阶段一：HTTP 升级握手};

\draw[arr] (0,-1.3) -- node[above, lbl]{HTTP GET, Upgrade: websocket} (5.5,-1.3);
\draw[arr] (5.5,-1.9) -- node[above, lbl]{101 Switching Protocols} (0,-1.9);

% ---- Phase 2: 身份认证 ----
\draw[dashline] (-1.6,-2.8) -- (12.6,-2.8);
\node[phaselbl, right] at (-1.6,-2.5) {阶段二：Token 认证};

\draw[arr] (0,-3.3) -- node[above, lbl]{\{join, orderID, token\}} (5.5,-3.3);
\draw[arr] (5.5,-3.9) -- node[above, lbl]{验证 Token → 通过} (0,-3.9);
\draw[arr] (5.5,-4.5) -- node[above, lbl]{加入 Room(orderID)} (11,-4.5);

% ---- Phase 3: 消息交互 ----
\draw[dashline] (-1.6,-5.5) -- (12.6,-5.5);
\node[phaselbl, right] at (-1.6,-5.2) {阶段三：全双工消息交互};

\draw[arr] (0,-6.0) -- node[above, lbl]{发送消息帧} (5.5,-6.0);
\draw[arr] (5.5,-6.6) -- node[above, lbl]{加密存储 + 广播} (11,-6.6);
\draw[arr] (11,-7.2)-- node[above, lbl]{推送给房间成员} (0,-7.2);

% ---- Phase 4: 连接关闭 ----
\draw[dashline] (-1.6,-8.0) -- (12.6,-8.0);
\node[phaselbl, right] at (-1.6,-7.7) {阶段四：连接关闭};

\draw[arr] (0,-8.5) -- node[above, lbl]{Close Frame} (5.5,-8.5);
\draw[arr] (5.5,-9.1) -- node[above, lbl]{Close ACK + Merkle 快照} (0,-9.1);

\end{tikzpicture}
\caption{WebSocket 握手及订单通信时序示意图}
\label{fig:websocket_seq}
\end{figure}
